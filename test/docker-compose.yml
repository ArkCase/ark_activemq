version: "3.7"

services:
  activemq:
    image: activemq:5.16.2-dev1
    build:
      context: ..
    init: true
    ports:
      - 5556:5556
      - 8161:8161
      - 61616:61616
    volumes:
      - ./conf:/app/conf:ro
